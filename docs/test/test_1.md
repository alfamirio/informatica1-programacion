# Preguntas Básicas de Programación en Python

1.  ¿Qué hace la función `print()` en Python?
    1. Lee texto del usuario.
    2. Muestra texto en la pantalla.
    3. Realiza cálculos matemáticos.
    4. Guarda información en un archivo.

2.  ¿Cómo se empieza un comentario en Python en una sola línea?
    1. Usando `*`
    2. Usando `//`
    3. Usando `#`
    4. Usando `(`

3.  ¿Cuál de estas es una palabra que Python entiende de forma especial (palabra reservada)?
    1. `variable`
    2. `number`
    3. `if`
    4. `text`

4.  Para guardar el número 5 en una caja llamada `contador`, ¿cómo lo escribes en Python?
    1. `contador is 5`
    2. `contador equals 5`
    3. `contador: 5`
    4. `contador = 5`

5.  Si tienes un número sin decimales como el 10, ¿qué tipo de dato es en Python?
    1. Texto (string)
    2. Número decimal (float)
    3. Número entero (int)
    4. Verdadero/Falso (bool)

6.  ¿Qué usas para pedir al usuario que escriba algo en la consola?
    1. `display()`
    2. `write()`
    3. `input()`
    4. `show()`

7.  ¿Cuál es el valor que significa "verdadero" en Python?
    1. `True`
    2. `Ok`
    3. `Sí`
    4. `1`

8.  ¿Cómo creas una variable llamada `saludo` con el texto "Hola"?
    1. `string saludo = "Hola"`
    2. `text saludo is "Hola"`
    3. `saludo = "Hola"`
    4. `create saludo "Hola"`

9.  Si intentas nombrar una variable con una palabra reservada como `if`, ¿qué pasa?
    1. Funciona bien.
    2. Python te corrige el nombre.
    3. Da un error.
    4. Python lo ignora.

10. ¿Cómo se escriben comentarios que ocupan varias líneas?
    1. Poniendo `#` al inicio de cada línea.
    2. Encerrando el texto entre `---`.
    3. Encerrando el texto entre `***`.
    4. Encerrando el texto entre comillas triples (`'''` o `"""`).

11. ¿Qué tipo de dato es "Ejemplo de texto"?
    1. `int`
    2. `float`
    3. `str`
    4. `bool`

12. ¿Cuál es el resultado de `2 + 3`?
    1. 2
    2. 3
    3. 5
    4. 6

13. Si tienes el texto "10" y quieres convertirlo al número 10, ¿qué función usas?
    1. `str()`
    2. `float()`
    3. `int()`
    4. `text_to_number()`

14. ¿Qué operador matemático usas para sumar?
    1. `-`
    2. `+`
    3. `*`
    4. `/`

15. ¿Cuál de estos nombres de variable está bien escrito en Python?
    1. `mi variable`
    2. `1variable`
    3. `mi_variable`
    4. `mi-variable`

16. En Python, no necesitas decir qué tipo de dato va a tener una variable cuando la creas. ¿Cómo se llama a esto?
    1. Tipado estático
    2. Tipado dinámico
    3. Tipado fuerte
    4. Tipado débil

17. ¿Qué valor representa "nada" o "ausencia de valor" en Python?
    1. `Zero`
    2. `Null`
    3. `None`
    4. `Empty`

18. Para unir dos textos (cadenas de texto), ¿qué operador usas?
    1. `&`
    2. `+`
    3. `-`
    4. `*`

19. ¿Qué función te dice de qué tipo es una variable (si es texto, número, etc.)?
    1. `print()`
    2. `show_type()`
    3. `type()`
    4. `info()`

20. En Python, las "constantes" (valores que no deberían cambiar) se suelen escribir...
    1. En minúsculas.
    2. Empezando con un número.
    3. En MAYÚSCULAS.
    4. Con un guion al principio.

21. Si tienes el texto "3.14" y quieres convertirlo al número decimal 3.14, ¿qué función usas?
    1. `int()`
    2. `str()`
    3. `float()`
    4. `decimal()`

22. Para preguntar si dos valores son exactamente iguales, ¿qué operador usas?
    1. `=`
    2. `==`
    3. `!=`
    4. `is_equal`

23. ¿Cuál es el resultado de `print("Hola" + "Mundo")`?
    1. `Hola Mundo`
    2. `HolaMundo`
    3. `Hola+Mundo`
    4. Error

24. Si una variable guarda `True` o `False`, ¿qué tipo de dato es?
    1. `int`
    2. `str`
    3. `bool`
    4. `float`

25. La función `input()` siempre lee la entrada del usuario como qué tipo de dato?
    1. `int`
    2. `float`
    3. `str`
    4. `bool`

26. ¿Qué operador usas para dividir un número entre otro y obtener un resultado con decimales si los hay?
    1. `//`
    2. `/`
    3. `%`
    4. `div`

27. ¿Cuál de estos NO es uno de los tipos de datos más básicos y simples en Python?
    1. Número entero (`int`)
    2. Texto (`str`)
    3. Lista (`list`)
    4. Verdadero/Falso (`bool`)

28. ¿Qué operador usas para la multiplicación?
    1. `+`
    2. `-`
    3. `*`
    4. `/`

29. Si tienes `x = 5` y luego escribes `x = x + 1`, ¿qué valor tiene `x` ahora?
    1. 5
    2. 6
    3. Error
    4. 51

30. ¿Qué mostrará `print(type("Casa"))`?
    1. `<class 'int'>`
    2. `<class 'str'>`
    3. `<class 'bool'>`
    4. `<class 'float'>`

31. Dentro de un texto, si quieres poner una comilla doble `"` pero el texto ya usa comillas dobles para empezar y terminar, ¿qué pones antes de la comilla interna? Por ejemplo: `"Esto es una \"cita\""`
    1. `/`
    2. `\`
    3. `#`
    4. `^`

32. Si tienes un texto como "Python", ¿qué hace la función `len("Python")`?
    1. Muestra "Python".
    2. Muestra la primera letra, 'P'.
    3. Dice cuántas letras tiene (6).
    4. Convierte el texto a mayúsculas.

33. ¿Cuál de estos nombres de variable NO está bien escrito en Python?
    1. `_saldo`
    2. `edad_usuario`
    3. `miVariable`
    4. `for`

34. ¿Qué operador usas para dividir y obtener solo la parte entera del resultado?
    1. `/`
    2. `//`
    3. `%`
    4. `int_div`

35. Si tienes el texto `"True"` y quieres convertirlo al valor booleano `True`, ¿qué función usas (pensando en conversiones comunes)?
    1. `int()`
    2. `str()`
    3. `bool()`
    4. `to_bool()`

36. ¿Cuál es la forma correcta de escribir el valor `False` en Python?
    1. `false`
    2. `FALSE`
    3. `False`
    4. `0`

37. ¿Es necesario poner un punto y coma `;` al final de cada línea de código en Python?
    1. Sí, siempre.
    2. No, generalmente no es necesario.
    3. Solo en las líneas con `print()`.
    4. Solo si la línea es muy larga.

38. Para preguntar si dos valores son DIFERENTES, ¿qué operador usas?
    1. `=`
    2. `==`
    3. `!=`
    4. `not_equal`

39. ¿Puedes asignar un valor a varias variables a la vez en una sola línea? Por ejemplo, que `x` sea 0, `y` sea 0 y `z` sea 0.
    1. No, debes hacerlo línea por línea.
    2. Sí, por ejemplo: `x = y = z = 0`.
    3. Sí, por ejemplo: `x, y, z = 0`.
    4. Sí, por ejemplo: `set x, y, z to 0`.

40. ¿Qué mostrará `print(type(10.5))`?
    1. `<class 'int'>`
    2. `<class 'str'>`
    3. `<class 'bool'>`
    4. `<class 'float'>`

41. Si tienes un bloque de código (como dentro de un `if` o un bucle) pero no quieres que haga nada por ahora, ¿qué palabra clave usas?
    1. `skip`
    2. `empty`
    3. `pass`
    4. `nothing`

42. Si escribes `# Esto es un comentario`, ¿el intérprete de Python intenta ejecutar esa línea?
    1. Sí, la ejecuta como una instrucción especial.
    2. No, la ignora por completo.
    3. Solo la ejecuta si hay un error.
    4. La guarda para mostrarla al final.

43. Si tienes el número 9.99, ¿qué tipo de dato es?
    1. `int`
    2. `str`
    3. `bool`
    4. `float`

44. ¿Qué operador usas para comprobar si un valor es menor que otro?
    1. `>`
    2. `<`
    3. `>=`
    4. `<=`

45. Si intentas usar una variable que todavía no le has dado ningún valor, ¿qué pasa?
    1. Python le pone el valor 0 automáticamente.
    2. Da un error porque la variable no existe.
    3. Python le pone una cadena vacía `""`.
    4. Funciona, pero su valor es desconocido.

46. ¿Puedes poner un espacio en el nombre de una variable? Por ejemplo: `mi variable = 10`.
    1. Sí.
    2. No, los nombres de variables no pueden tener espacios.
    3. Solo si usas comillas.
    4. Solo si es una constante.

47. ¿Qué hace la función `input()` si el usuario simplemente presiona Enter sin escribir nada?
    1. Devuelve el valor `None`.
    2. Da un error.
    3. Devuelve una cadena vacía `""`.
    4. Devuelve el número `0`.

48. Para mostrar varias cosas con un solo `print()`, separadas por un espacio automáticamente, ¿cómo las pones dentro del paréntesis? Por ejemplo, mostrar "Mi edad es" y luego el valor de la variable `edad`.
    1. `print("Mi edad es" + edad)`
    2. `print("Mi edad es", edad)`
    3. `print("Mi edad es" - edad)`
    4. `print("Mi edad es" * edad)`

49. ¿Cuál es la principal razón para usar comentarios en tu código?
    1. Para decorar el código.
    2. Para hacer que el código sea más difícil de leer.
    3. Para explicar a las personas cómo funciona el código.
    4. Para que el código se ejecute más rápido.

50. Si tienes `numero = 7` y luego escribes `numero = numero * 2`, ¿qué valor tiene `numero` ahora?
    1. 7
    2. 9
    3. 14
    4. Error

---

## Respuestas Correctas

| Pregunta | Respuesta | | Pregunta | Respuesta | | Pregunta | Respuesta | | Pregunta | Respuesta | | Pregunta | Respuesta |
| :------- | :-------- | | :------- | :-------- | | :------- | :-------- | | :------- | :-------- | | :------- | :-------- |
| 1        | 2         | | 11       | 3         | | 21       | 3         | | 31       | 2         | | 41       | 3         |
| 2        | 3         | | 12       | 3         | | 22       | 2         | | 32       | 3         | | 42       | 2         |
| 3        | 3         | | 13       | 3         | | 23       | 2         | | 33       | 4         | | 43       | 4         |
| 4        | 4         | | 14       | 2         | | 24       | 3         | | 34       | 2         | | 44       | 2         |
| 5        | 3         | | 15       | 3         | | 25       | 3         | | 35       | 3         | | 45       | 2         |
| 6        | 3         | | 16       | 2         | | 26       | 2         | | 36       | 3         | | 46       | 2         |
| 7        | 1         | | 17       | 3         | | 27       | 3         | | 37       | 2         | | 47       | 3         |
| 8        | 3         | | 18       | 2         | | 28       | 3         | | 38       | 3         | | 48       | 2         |
| 9        | 3         | | 19       | 3         | | 29       | 2         | | 39       | 2         | | 49       | 3         |
| 10       | 4         | | 20       | 3         | | 30       | 2         | | 40       | 4         | | 50       | 3         |
